/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['files.catbox.moe','ik.imagekit.io','i.pravatar.cc','img10.hotstar.com','img.hotstar.com'],  // ✅ Allow Catbox images
      },
      
};

export default nextConfig;
